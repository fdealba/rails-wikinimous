  <% @articles.each do |article| %>
  <div class="mb-4">
    <h2 class="p-0"><%= link_to article.title, article_path(article), class: "text-info" %></h2>
    <p class="font-weight-lighter"><%= article.content %></p>
  </div>
  <% end %>
</div>

