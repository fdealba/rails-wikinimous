<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.10/css/all.css">
<div class="d-flex align-content-end border-bottom mb-5">
    <h1><%= @article.title %></h1>
    <div class="d-flex m-2">
    <h4><%= link_to raw('<i class="fas fa-pencil-alt"></i>'), edit_article_path(@article) %></h4>
    <h4> <%= link_to raw('<i class="fas fa-trash"></i>'), @article_path, method: :delete, data: { confirm: "Are you sure?" } %></h4>
    </div>
</div>


  <p class="text-left"><%= @article.content %></p>
  <%= link_to "Back to list", articles_path %>
